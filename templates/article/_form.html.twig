{{ form_start(form) }}
    <div class="form-group">
        <label for="">Titre :</label> {{ form_widget(form.titre,{'attr':{'class':'form-control'}}) }}
    </div>
    <div class="form-group">
        <label for="">Contenu :</label> {{ form_widget(form.contenu,{'attr':{'class':'form-control','rows':'16'}}) }}
    </div>
    <div class="form-group">
        <label for="">Sélectionner une image :</label> {{ form_widget(form.image) }}
    </div>
    <button class="btn btn-primary">{{ button_label|default('Enregister') }}</button>
{{ form_end(form) }}
