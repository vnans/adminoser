{% extends 'base.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block body %}
<div class="row">
  <div class="col-md-12">
    <h1>Modifier l'Article</h1>

  <p>{{ include('article/_form.html.twig', {'button_label': 'Enrégistrer les modifications'}) }}</p>
    <p>{{ include('article/_delete_form.html.twig') }}</p>
    <a href="{{ path('article_index') }}">Retour à la liste</a>
  </div>


</div>
{% endblock %}
