{% extends 'baseweb.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
<body>


<section id="about">
    <div class="container">
<p></p>
<p></p>
      <header class="section-header">
        <h3>{{ article.titre }}</h3>
        <p>
    <!-- <b> {{ article.titre }}</b> -->
        </p>
      </header>



      <div class="row about-extra">
        <div class="col-lg-6 wow fadeInUp">
          <img src="{{ asset('img/')}}{{ article.image }} " class="img-fluid" alt="">
        </div>
        <div class="col-lg-6 wow fadeInUp pt-5 pt-lg-0">
          <h4>{{ article.titre }}.</h4>
          <p>
          {{ article.contenu }}
          </p>

        </div>
      </div>



    </div>
  </section><!-- #about -->






{#
    <h1>Article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ article.contenu }}</td>
            </tr>
            <tr>
                <th>Ladate</th>
                <td>{{ article.ladate ? article.ladate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ article.image }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ article.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_index2') }}">back to list</a>

    <a href="{{ path('article_edit', {'id': article.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }} #}
    <a href="{{ path('article_index2') }}">Retour à la liste</a>
</body>
{% endblock %}
